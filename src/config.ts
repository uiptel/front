/* global process */
/* global __webpack_hash__ */

export default {
    appVersion: '0.0.1', // process.env.APP_VERSION,
    // eslint-disable-next-line @typescript-eslint/camelcase
    buildHash: 'local', // __webpack_hash__,
    debug: true, // process.env.NODE_ENV !== 'production',
};
